(executables
 (names pedro)
 (libraries ocamlgraph))

(ocamllex
  (modules lexer))

(menhir
  (modules parser)
  (flags "--explain"))

(install
 (section bin)
 (files
  (pedro.exe as pedro))
 (package pedro))
