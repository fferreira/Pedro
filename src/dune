(executables
 (names pn)
 (libraries ocamlgraph))

(ocamllex
  (modules lexer))

(menhir
  (modules parser)
  (flags "--explain"))

(install
 (section bin)
 (files
  (pn.exe as pn))
 (package pedro))
