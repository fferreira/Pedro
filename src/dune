(executables
 (names pedro)
 (libraries ocamlgraph sexplib)
 (modes (best exe) (best object) (best shared_object)))

(ocamllex
  (modules lexer))

(menhir
  (modules parser)
  (flags "--explain"))

(install
 (section bin)
 (files
  (pedro.exe as pedro))
 (package pedro))
